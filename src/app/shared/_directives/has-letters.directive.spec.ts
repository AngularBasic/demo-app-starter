import { HasLettersDirective } from './has-letters.directive';

describe('HasLettersDirective', () => {
  it('should create an instance', () => {
    const directive = new HasLettersDirective();
    expect(directive).toBeTruthy();
  });
});
